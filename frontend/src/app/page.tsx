import LoadingPage from '@/components/marketing/loading/LoadingPage';

// Root now renders the marketing loading page (the true root of the site).
export default function RootPage() {
  return <LoadingPage />;
}
