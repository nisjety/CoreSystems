# Pinned to option (a) below — VERIFIED WORKING 2026-07-17 on Apple MPS.
#
# History: the previous file requested colpali-engine>=0.3.17 + transformers>=5,
# but pip resolved transformers 4.57.x (colpali 0.3.17's own bounds conflict
# with >=5 in practice), which breaks at import time: colpali 0.3.17
# unconditionally imports `ModernVBertModel`, absent before transformers 5.
# Additionally the `vidore/colqwen2.5-v0.2` adapter's attention/MLP LoRA was
# trained against the pre-v5 Qwen2.5-VL key layout (`model.layers.*`); on
# transformers 5 those deltas don't re-map (functional, but not fully
# fine-tuned). For FULL LoRA fidelity the consistent trios are:
#   (a) colpali-engine==0.3.10 + transformers 4.51.x + vidore/colqwen2.5-v0.2  ← pinned
#   (b) colpali-engine>=0.3.17 + transformers>=5 + a v5-compatible adapter revision.
# colpali-engine 0.3.10 declares transformers>=4.51.1,<4.52 and torch>=2.5,<2.7.
colpali-engine==0.3.10
transformers==4.51.3
peft==0.15.2
qwen-vl-utils>=0.0.8
fastapi>=0.110
uvicorn[standard]>=0.27
pillow>=10.0
httpx>=0.27
accelerate>=1.0
# torch is installed separately (CUDA wheel in Docker; native MPS wheel locally).
# Local (MPS): pip install 'torch==2.6.0' 'torchvision==0.21.0'
