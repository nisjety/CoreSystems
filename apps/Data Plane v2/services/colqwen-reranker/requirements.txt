colpali-engine>=0.3.17
# VERSION CONSTRAINT (verify at GPU-build time): colpali-engine 0.3.17 imports
# `ModernVBertModel`, which only exists in transformers >=5 — so transformers
# must be >=5 for the import to succeed. HOWEVER, the `vidore/colqwen2.5-v0.2`
# adapter's attention/MLP LoRA was trained against the pre-v5 Qwen2.5-VL key
# layout (`model.layers.*`); on transformers 5 those deltas don't re-map
# (the projection head DOES, so the model is functional but not fully
# fine-tuned). For FULL LoRA fidelity choose ONE consistent trio and confirm
# there are no "MISSING" LoRA keys in the load log:
#   (a) colpali-engine~=0.3.7 + transformers~=4.50 + vidore/colqwen2.5-v0.2, or
#   (b) colpali-engine>=0.3.17 + transformers>=5 + a v5-compatible adapter revision.
transformers>=5
qwen-vl-utils>=0.0.8
fastapi>=0.110
uvicorn[standard]>=0.27
pillow>=10.0
httpx>=0.27
# torch is installed separately (CUDA wheel in Docker; native MPS wheel locally).
