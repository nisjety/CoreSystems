fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
asyncpg>=0.30,<1
nats-py>=2.9,<3
redis>=5.2,<6
httpx>=0.28,<1
# U2-16 (velion ui-ux-velion-gap.md §10): web_fetch tool uses selectolax
# for fast HTML-to-text extraction. When missing the tool falls back to
# a naive regex stripper, so absence is non-fatal.
selectolax>=0.3,<1
websockets>=12,<14
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
croniter>=3.0,<4
structlog>=24.4,<26
openai>=1.40,<2
# Token estimation (optional — graceful fallback to char heuristic if absent)
tiktoken>=0.7,<1
# Phase 1 — PydanticAI + Temporal foundations
pydantic-ai>=0.1,<2
temporalio>=1.24,<2
# Phase 3 — LangGraph stateful workflows
langgraph>=0.2,<2
# Phase 5 — Letta long-term memory
letta-client>=0.1,<1
