# Product showcase provenance

`composer-norwegian-live.png` is a crop of the live local Verevon SPA on
11 September 2026. The prompt was entered for capture and **not submitted**.
It contains no account header, personal greeting, customer records, or history.

`product-showcase.mp4` and `product-showcase-poster.jpg` are generated by
`remotion/VerevonProductShowcase.tsx` (composition `Verevon-Product`). The abstract
background reuses `feature-film-backgrounds/connect-systems-pastel.jpg`; see that
folder's SOURCES.md. The animated labels describe a workflow, not a recorded run.

Render:

```sh
pnpm exec remotion render remotion/index.ts Verevon-Product public/verevon-product-shots/product-showcase.mp4 --codec=h264 --crf=21 --muted
pnpm exec remotion still remotion/index.ts Verevon-Product public/verevon-product-shots/product-showcase-poster.jpg --frame=100
```
