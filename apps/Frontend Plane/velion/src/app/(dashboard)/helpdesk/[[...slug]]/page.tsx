import { HelpdeskWorkspacePage } from '@/components/dashboard/product-section-pages';

export default function HelpdeskSectionPage({ params }: { params: { slug?: string[] } }) {
  return <HelpdeskWorkspacePage />;
}
