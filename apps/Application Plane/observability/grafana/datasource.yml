# Grafana datasource provisioning (Phase 6 B13) — points at the Application-Plane
# Prometheus so dashboards work out of the box with no manual setup.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://application-prometheus:9090
    isDefault: true
    editable: true
