# orchestrator-core

Temporal workflow and activity service for the **Model Plane** subsystem of CoreSystem.

---

## Overview

`orchestrator-core` drives multi-step AI run orchestration using [Temporal](https://temporal.io/). It exposes Temporal workflows and activities that coordinate session management, step execution, sandboxing, capabilities, and browser control across the Model Plane microservices.

---

## Architecture

### Proto-based gRPC clients (current)

All inter-service communication uses **buf-generated, typed gRPC proto clients**. The old JSON shim (`internal/rpc/`) has been deleted.

```
go/gen/model_plane/v1/          ← generated by `buf generate`
    sessions.pb.go
    sessions_grpc.pb.go
    execution.pb.go
    execution_grpc.pb.go
    ...

go/services/orchestrator-core/
    cmd/
        activities/
            activities.go       ← typed proto clients; NO JSON shim
        worker/
            main.go
    internal/
        config/
            config.go
        grpcclient/
            clients.go          ← grpcclient.Clients struct wires all proto clients
```

Import alias used throughout activities:

```go
mpv1 "github.com/triodelab/model-plane/gen/go/model_plane/v1"
```

### Deleted shim

`internal/rpc/` (the old `rpc.Invoke`-based JSON-over-HTTP shim) has been **permanently removed**. Do not re-add it.

---

## Activities

All activities are **fully implemented** with typed gRPC proto clients. There are no phantom stubs.

### Run lifecycle

| Activity | Downstream client | Description |
|---|---|---|
| `StartRunActivity` | `mpv1.NewSessionCoreClient` | Creates a new run session; falls back to local `RunMetadata` on `codes.Unavailable` |
| `ExecuteStepLoopActivity` | `mpv1.NewExecutionCoreClient` | Loops up to `MaxTurns` calling `ExecuteStep`; breaks on `completed` / `awaiting_approval` |
| `CompleteRunActivity` | `natsx.Publisher` | Emits `RUN_COMPLETED` envelope (idempotency key `<runID>:completed`) |
| `FailRunActivity` | `natsx.Publisher` | Emits `RUN_FAILED` envelope (idempotency key `<runID>:failed`) |

### Memory consolidation

| Activity | Downstream client | Description |
|---|---|---|
| `QueryMemoryEntriesActivity` | `mpv1.NewMemoryServiceClient` (letta-bridge) | `SearchMemory` with optional `Since` filter |
| `SummarizeMemoryActivity` | local helper `summarizeMemoryEntries` | Groups by `ThreadID`; truncates each summary to 512 chars |
| `WriteConsolidatedMemoryActivity` | `mpv1.NewMemoryServiceClient` (letta-bridge) | Calls `IndexMemory` per consolidated entry |

### Skill promotion

| Activity | Downstream client | Description |
|---|---|---|
| `ValidateSkillBundleActivity` | `mpv1.NewCapabilityCoreClient` | `ValidateSkillBundle` on signed bundle |
| `RunPromotionGateActivity` | `mpv1.NewCapabilityCoreClient` | `CheckSkillPromotion` gate |
| `UpdateRegistryActivity` | `mpv1.NewCapabilityCoreClient` | `PromoteSkill` to the registry |

### Helpers

- `publishRunEvent(ctx, runID, eventType, payload, suffix)` — builds an `envelope.Envelope` with a UUID `EventID`, schema version `1`, producer `"orchestrator-core"`, idempotency key `<runID>:<suffix>`, validates + encodes, then publishes to `natsx.RunEventSubject(runID)`.
- `summarizeMemoryEntries(entries)` — deterministic local consolidation used by `SummarizeMemoryActivity`.

### Deferred scope (not implemented)

The following activity surfaces are not present in `activities.go` and are deferred to later slices:

- Sandbox allocate/release (sandbox-manager)
- Browser launch/close (browser-broker)
- Direct Letta bridging outside the memory consolidation flow
- Run-metric recording (observability pipeline)

---

## Starting a workflow (`OrchestratorWorkflowService.StartWorkflow`)

The gRPC listener (`ORCHESTRATOR_GRPC_ADDR`, default `:9080`) serves two services:

| Service | Role |
|---|---|
| `OrchestrationCoreService` | Read/transition proxy to session-core. Forwards the caller's credential upstream; session-core is the authority. |
| `OrchestratorWorkflowService` | `StartWorkflow` — the production entry point into the durable Temporal tier. Authenticated and authorized **locally**. |

`StartWorkflow` is a separate service rather than another method on
`OrchestrationCoreService` because session-core (Rust) implements every method of
that service; adding one there would force it to implement a Temporal start it
does not own.

### Allowlist

`workflow_type` must appear in `internal/orchestration/workflowreg.go`'s
allowlist, which `cmd/workflow_parity_test.go` asserts is exactly the set
`registeredWorkflows()` registers on the worker. Temporal always receives the
allowlist's canonical constant, never the client's string.

`AutoresearchWorkflow` is deliberately in neither list: its `budget_usd` guard
multiplies a hardcoded $0.10 by the step count because `ExecuteStepResponse`
carries no usage data, so exposing it would advertise a spending cap that is not
enforced.

### Authorization

Two credentials are accepted; with neither configured the RPC is not even
registered and answers `Unimplemented`.

1. **Auth Core JWT** (`ORCHESTRATOR_CORE_AUTH_AUDIENCES`, `AUTH_CORE_ISSUER`,
   `AUTH_CORE_JWKS_URL`). Tenancy comes from the verified principal and
   overwrites whatever `input` carries; a conflicting `org_id` is denied.
   User principals may start run-scoped workflows; service principals need
   `orchestration:workflow:start`, plus `orchestration:workflow:start:global`
   for registry-wide ones. `user_id` from a service principal is refused — a
   workload run is org-scoped, never viewer-scoped.
2. **Model-Plane-local internal secret**
   (`ORCHESTRATOR_INTERNAL_SERVICE_TOKEN` +
   `ORCHESTRATOR_INTERNAL_SERVICE_ORGS`), presented in the
   `x-model-plane-internal-token` metadata header. For hops with no live
   per-user bearer — capability-core's cron task dispatcher. Both variables are
   required: there is no wildcard org, so a leaked secret cannot reach another
   tenant. It can never start registry-wide workflows.

### Workflow id

`mp-wf/<Type>/<sha256(org_id)[:32]>/<anchor>` where the anchor is `run_id`, or
`idempotency_key` when supplied. Started with
`WORKFLOW_ID_CONFLICT_POLICY_USE_EXISTING`, so a retried or redelivered start
re-attaches to the existing execution instead of double-starting a run. The org
is hashed (not interpolated) because org ids come from a JWT claim: hashing keeps
the id bounded and separator-safe while still guaranteeing two organizations can
never collide.

`run_id` must be a single NATS subject token. Run lifecycle events publish to
`mp.v1.run.<run_id>.event` and consumers subscribe to `mp.v1.run.*.event`, where
`*` matches exactly one token — a dotted run id would publish RUN_COMPLETED where
nothing is listening.

---

## Proto generation

Generated files live in the `gen` module:

```
go/gen/model_plane/v1/
```

To regenerate after `.proto` changes, run from the repo root (where `buf.gen.yaml` lives):

```bash
buf generate
```

The generated Go module path is:

```
github.com/triodelab/model-plane/gen/go
```

This is declared as a `replace` directive in `orchestrator-core/go.mod` pointing to `../../gen`.

---

## Building

> **Important**: `go/` is a Go workspace root (contains `go.work`), **not** a Go module. You cannot run `go build` directly from `go/`. Always `cd` into a module directory first.

### Build this service only

```bash
cd "apps/Model Plane/go/services/orchestrator-core"
go build ./...
```

### Build all 9 workspace modules

```bash
BASE="apps/Model Plane/go"
for dir in \
  gen \
  pkg/envelope \
  pkg/idempotency \
  pkg/natsx \
  services/browser-broker \
  services/capability-core \
  services/letta-bridge \
  services/orchestrator-core \
  services/sandbox-manager; do
  echo "=== $dir ===" && (cd "$BASE/$dir" && go build ./...) || { echo "FAILED: $dir"; break; }
done && echo "ALL OK"
```

---

## Go workspace modules

| Module path | Directory |
|---|---|
| `github.com/triodelab/model-plane/gen/go` | `go/gen` |
| `github.com/triodelab/model-plane/pkg/envelope` | `go/pkg/envelope` |
| `github.com/triodelab/model-plane/pkg/idempotency` | `go/pkg/idempotency` |
| `github.com/triodelab/model-plane/pkg/natsx` | `go/pkg/natsx` |
| `github.com/triodelab/model-plane/services/browser-broker` | `go/services/browser-broker` |
| `github.com/triodelab/model-plane/services/capability-core` | `go/services/capability-core` |
| `github.com/triodelab/model-plane/services/letta-bridge` | `go/services/letta-bridge` |
| `github.com/triodelab/model-plane/services/orchestrator-core` | `go/services/orchestrator-core` |
| `github.com/triodelab/model-plane/services/sandbox-manager` | `go/services/sandbox-manager` |

Go workspace version: `go 1.25.1` (declared in `go/go.work`)

---

## Configuration

Key environment variables (defaults in `internal/config/config.go`):

| Variable | Default | Description |
|---|---|---|
| `TEMPORAL_HOST` | `localhost:7233` | Temporal server address |
| `SESSION_CORE_ADDR` | `localhost:50051` | session-core gRPC endpoint |
| `EXECUTION_CORE_ADDR` | `localhost:50052` | execution-core gRPC endpoint |
| `CAPABILITY_CORE_ADDR` | `localhost:50053` | capability-core gRPC endpoint |
| `SANDBOX_MANAGER_ADDR` | `localhost:50054` | sandbox-manager gRPC endpoint |
| `BROWSER_BROKER_ADDR` | `localhost:50055` | browser-broker gRPC endpoint |
| `LETTA_BRIDGE_ADDR` | `localhost:50056` | letta-bridge gRPC endpoint |
| `ORCHESTRATOR_GRPC_ADDR` | `:9080` | gRPC listener for both served services |
| `ORCHESTRATOR_CORE_AUTH_AUDIENCES` | `orchestrator-core` | Comma-separated token audiences `StartWorkflow` accepts |
| `ORCHESTRATOR_INTERNAL_SERVICE_TOKEN` | — | Model-Plane-local secret for `StartWorkflow`; requires the org list below |
| `ORCHESTRATOR_INTERNAL_SERVICE_ORGS` | — | Comma-separated orgs that secret may start work for; no wildcard |

---

## Dependencies

- [go.temporal.io/sdk](https://pkg.go.dev/go.temporal.io/sdk) — workflow and activity runtime
- [google.golang.org/grpc v1.80.0](https://pkg.go.dev/google.golang.org/grpc) — gRPC transport
- `github.com/triodelab/model-plane/gen/go` — buf-generated proto clients (local replace)
