// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: model_plane/v1/routing_policy.proto

package mpv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	RoutingPolicy_GetPolicy_FullMethodName = "/model_plane.v1.RoutingPolicy/GetPolicy"
	RoutingPolicy_SetPolicy_FullMethodName = "/model_plane.v1.RoutingPolicy/SetPolicy"
)

// RoutingPolicyClient is the client API for RoutingPolicy service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// RoutingPolicy — runtime-tunable configuration for the Velion intent layer
// (the "model router"). The intent layer in inference-core resolves the
// velion-budget / velion-balance / velion-genius modes to a concrete model
// from request complexity + budget posture; this service makes that policy
// editable at runtime instead of compile-time constants.
//
// session-core owns the durable store (a JSONB singleton row). The full policy
// travels as an opaque JSON string in `config_json` — the canonical schema is
// owned by inference-core's RoutingPolicy struct (snake_case). An empty/`{}`
// config means "use the built-in defaults" (the values hard-coded today), so a
// fresh deployment behaves identically until an admin edits the policy.
//
// Read path: inference-core polls GetPolicy on a refresh tick (fail-soft to
// defaults). Write path: the Velion BFF gateway calls SetPolicy on an admin
// PUT. The gateway's read for the admin UI hits inference-core's effective-view
// endpoint, not this service, so the UI shows what routing is actually using.
type RoutingPolicyClient interface {
	// Fetch the current policy (singleton). Returns the seeded empty config until
	// the first SetPolicy write.
	GetPolicy(ctx context.Context, in *GetRoutingPolicyRequest, opts ...grpc.CallOption) (*RoutingPolicyMessage, error)
	// Replace the policy. `config_json` must be valid JSON (validated like
	// finetune hyperparameters). `version` is server-incremented; `updated_at`
	// is server-stamped.
	SetPolicy(ctx context.Context, in *SetRoutingPolicyRequest, opts ...grpc.CallOption) (*RoutingPolicyMessage, error)
}

type routingPolicyClient struct {
	cc grpc.ClientConnInterface
}

func NewRoutingPolicyClient(cc grpc.ClientConnInterface) RoutingPolicyClient {
	return &routingPolicyClient{cc}
}

func (c *routingPolicyClient) GetPolicy(ctx context.Context, in *GetRoutingPolicyRequest, opts ...grpc.CallOption) (*RoutingPolicyMessage, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RoutingPolicyMessage)
	err := c.cc.Invoke(ctx, RoutingPolicy_GetPolicy_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *routingPolicyClient) SetPolicy(ctx context.Context, in *SetRoutingPolicyRequest, opts ...grpc.CallOption) (*RoutingPolicyMessage, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RoutingPolicyMessage)
	err := c.cc.Invoke(ctx, RoutingPolicy_SetPolicy_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// RoutingPolicyServer is the server API for RoutingPolicy service.
// All implementations must embed UnimplementedRoutingPolicyServer
// for forward compatibility.
//
// RoutingPolicy — runtime-tunable configuration for the Velion intent layer
// (the "model router"). The intent layer in inference-core resolves the
// velion-budget / velion-balance / velion-genius modes to a concrete model
// from request complexity + budget posture; this service makes that policy
// editable at runtime instead of compile-time constants.
//
// session-core owns the durable store (a JSONB singleton row). The full policy
// travels as an opaque JSON string in `config_json` — the canonical schema is
// owned by inference-core's RoutingPolicy struct (snake_case). An empty/`{}`
// config means "use the built-in defaults" (the values hard-coded today), so a
// fresh deployment behaves identically until an admin edits the policy.
//
// Read path: inference-core polls GetPolicy on a refresh tick (fail-soft to
// defaults). Write path: the Velion BFF gateway calls SetPolicy on an admin
// PUT. The gateway's read for the admin UI hits inference-core's effective-view
// endpoint, not this service, so the UI shows what routing is actually using.
type RoutingPolicyServer interface {
	// Fetch the current policy (singleton). Returns the seeded empty config until
	// the first SetPolicy write.
	GetPolicy(context.Context, *GetRoutingPolicyRequest) (*RoutingPolicyMessage, error)
	// Replace the policy. `config_json` must be valid JSON (validated like
	// finetune hyperparameters). `version` is server-incremented; `updated_at`
	// is server-stamped.
	SetPolicy(context.Context, *SetRoutingPolicyRequest) (*RoutingPolicyMessage, error)
	mustEmbedUnimplementedRoutingPolicyServer()
}

// UnimplementedRoutingPolicyServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRoutingPolicyServer struct{}

func (UnimplementedRoutingPolicyServer) GetPolicy(context.Context, *GetRoutingPolicyRequest) (*RoutingPolicyMessage, error) {
	return nil, status.Error(codes.Unimplemented, "method GetPolicy not implemented")
}
func (UnimplementedRoutingPolicyServer) SetPolicy(context.Context, *SetRoutingPolicyRequest) (*RoutingPolicyMessage, error) {
	return nil, status.Error(codes.Unimplemented, "method SetPolicy not implemented")
}
func (UnimplementedRoutingPolicyServer) mustEmbedUnimplementedRoutingPolicyServer() {}
func (UnimplementedRoutingPolicyServer) testEmbeddedByValue()                       {}

// UnsafeRoutingPolicyServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RoutingPolicyServer will
// result in compilation errors.
type UnsafeRoutingPolicyServer interface {
	mustEmbedUnimplementedRoutingPolicyServer()
}

func RegisterRoutingPolicyServer(s grpc.ServiceRegistrar, srv RoutingPolicyServer) {
	// If the following call panics, it indicates UnimplementedRoutingPolicyServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&RoutingPolicy_ServiceDesc, srv)
}

func _RoutingPolicy_GetPolicy_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetRoutingPolicyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(RoutingPolicyServer).GetPolicy(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: RoutingPolicy_GetPolicy_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(RoutingPolicyServer).GetPolicy(ctx, req.(*GetRoutingPolicyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _RoutingPolicy_SetPolicy_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SetRoutingPolicyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(RoutingPolicyServer).SetPolicy(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: RoutingPolicy_SetPolicy_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(RoutingPolicyServer).SetPolicy(ctx, req.(*SetRoutingPolicyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// RoutingPolicy_ServiceDesc is the grpc.ServiceDesc for RoutingPolicy service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var RoutingPolicy_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "model_plane.v1.RoutingPolicy",
	HandlerType: (*RoutingPolicyServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "GetPolicy",
			Handler:    _RoutingPolicy_GetPolicy_Handler,
		},
		{
			MethodName: "SetPolicy",
			Handler:    _RoutingPolicy_SetPolicy_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "model_plane/v1/routing_policy.proto",
}
